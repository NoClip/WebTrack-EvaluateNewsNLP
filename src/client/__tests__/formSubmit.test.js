import { analyzeArticleClick } from '../js/formSubmit';

describe('Form handler test', () => {
    test('check that analyzeArticleClick is defined ', () => {
        expect(analyzeArticleClick).toBeDefined();
    });
});
